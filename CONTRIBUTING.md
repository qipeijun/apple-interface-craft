# 贡献说明

欢迎改进设计决策、平台适配与交互示例。提交前，请让改动与一个具体问题对应。

## Skill 内容

- 说明用户任务、目标平台和现有规则为什么不足。
- 区分官方要求、工程推断与项目选择；涉及版本行为时提供直接支持结论的官方来源。
- 保持 `SKILL.md` 精简，专题内容放入 `references/` 并建立可发现的引用。
- 不把某个框架、尺寸、API 或动效参数变成跨平台通用规定。
- 变更调用边界时说明哪些请求应该触发、哪些不应该触发。

## 落地页

- 入口为 `docs/index.html`，保持无需构建、可离线打开。
- 使用清晰的状态、真实反馈和可见退出路径；模拟任务需明确标注。
- 保留键盘操作、语义标签、减少动态与减少透明度路径。
- 页面演示不收集访问者输入，不添加分析脚本或外部资源依赖。

## 验证

安装开发依赖后运行 `npm test`。更改交互时补充相关的状态转换检查，避免只测试文案是否存在。

提交说明包含：问题、实际改动、已执行验证和未覆盖范围。浏览器与设备验收请注明环境；jsdom 不能证明视觉效果、原生模态焦点或真实输入体验。无需为文档改动执行构建。
